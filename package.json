{
  "name": "security-agent-benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "bench": "dist/src/cli.js"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "bench": "node dist/src/cli.js",
    "test": "node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
